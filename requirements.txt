sentence-transformers==6.0.1
pika==1.3.2
pydantic==1.10.9
boto3==1.26.13
cython
PyYAML
wheel>=0.38.1
# These four move together - see docker/SECURITY-EXCEPTIONS.md section 1.
#
# sentence-transformers 2.2.2 imported cached_download from huggingface_hub,
# which capped hub below 0.26 and so capped transformers at 4.x. 6.0.1 is the
# line built for transformers 5.x and huggingface_hub 1.x, which is what
# transformers 5.17.0 requires (hub >=1.5.0,<2.0).
#
# transformers 5.17.0 clears CVE-2026-5241 (critical, needs 5.5.0),
# CVE-2026-4372 (needs 5.3.0), CVE-2026-1839 (needs 5.0.0rc3), CVE-2025-6638
# (needs 4.53.0) and CVE-2026-9856 (needs 5.10.x), on top of the CVE-2024-3568
# critical that the earlier 4.48.0 bump cleared.
#
# transformers 5.x declares torch>=2.4, so this could not be taken while torch
# sat at 2.0.0. The torch pin lives in docker/Dockerfile; the two have to be
# bumped in the same commit or neither resolves.
transformers==5.17.0
huggingface_hub>=1.5.0,<2.0
requests>=2.31.0
cryptography>=41.0.0
azure-storage-blob>=12.9.0
azure-identity>=1.10.0
# Held at 1.x for fairseqForkSepFix, whose Cython extensions cimport numpy and
# are built against whatever is installed here. torch no longer forces this -
# 2.11.0 is fine with numpy 2 - but the fairseq fork has not been tried against
# the numpy 2 C-API, so bump it on its own and not as part of a security fix.
numpy<2
